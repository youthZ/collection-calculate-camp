'use strict';

function collect_min_number(collection) {
  'use strict';

function collect_min_number(collection) {
  return colleciton.reduce(function (x,y){
	return x < y ? x : y;
  });
}

module.exports = collect_min_number;
}

module.exports = collect_min_number;

